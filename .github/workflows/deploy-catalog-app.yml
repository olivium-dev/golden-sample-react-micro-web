name: Deploy Catalog App

on:
  push:
    branches: [ main ]
    paths:
      - 'frontend/catalog-app/**'
      - '.github/workflows/deploy-catalog-app.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'frontend/catalog-app/**'

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: |
        cd frontend/shared-ui-lib
        npm install
        cd ../catalog-app
        npm install
        
    - name: Build application
      run: |
        cd frontend/catalog-app
        npm run build
        
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: frontend/catalog-app/build
        destination_dir: catalog
