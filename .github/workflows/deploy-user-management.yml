name: Deploy User Management App

on:
  push:
    branches: [ main ]
    paths:
      - 'frontend/user-management-app/**'
      - '.github/workflows/deploy-user-management.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'frontend/user-management-app/**'

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Install dependencies
      run: |
        cd frontend/shared-ui-lib
        npm install
        cd ../user-management-app
        npm install
        
    - name: Build application
      run: |
        cd frontend/user-management-app
        # Increase Node.js memory limit and add timeout
        export NODE_OPTIONS="--max-old-space-size=4096"
        timeout 10m npm run build
        
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: frontend/user-management-app/build
        destination_dir: user-management
