import React from 'react';
import App from './app/App';

// Export the component for Module Federation (Data Grid has its own theme)
const DataGrid: React.FC = () => {
  return <App />;
};

export default DataGrid;
