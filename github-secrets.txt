# GitHub Secrets Configuration for SSH Password Authentication
# Copy these values to GitHub → Settings → Secrets and variables → Actions

# ==========================================
# SSH PASSWORD AUTHENTICATION SECRET
# ==========================================

Secret Name: CREAMAT_SSH_PASSWORD
Secret Value: P@ssw0rd768

# ==========================================
# SERVER CONNECTION DETAILS (for reference)
# ==========================================

Server IP: 192.168.2.73
SSH User: ec2-user
SSH Password: P@ssw0rd768
Server Hostname: golden-sample

# ==========================================
# SERVER CAPABILITIES DETECTED
# ==========================================

✅ Docker: v28.5.0 (installed and working)
❌ Docker Compose: Not installed (needs installation)
✅ Nginx: Running and active
✅ SSL Certificate: Available for dev-creamat.fds-1.com

# ==========================================
# WORKFLOW CONFIGURATION UPDATES NEEDED
# ==========================================

Update your workflow to use:
- Server: 192.168.2.73 (instead of vps-73.fds-1.com)
- SSH User: ec2-user
- SSH Password: Use secret CREAMAT_SSH_PASSWORD

# ==========================================
# ADDITIONAL SECRETS (if needed for full deployment)
# ==========================================

# For JWT authentication (if deploying the full micro-frontend app):
JWT_SECRET_KEY: your-jwt-secret-key-here
JWT_REFRESH_SECRET_KEY: your-jwt-refresh-secret-key-here

# ==========================================
# INSTALLATION COMMANDS FOR DOCKER COMPOSE
# ==========================================

# Run this on the server to install Docker Compose:
# sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
# sudo chmod +x /usr/local/bin/docker-compose
# docker-compose --version

# ==========================================
# COPY TO CLIPBOARD INSTRUCTIONS
# ==========================================

1. Copy the secret value: P@ssw0rd768
2. Go to: https://github.com/olivium-dev/golden-sample-react-micro-web/settings/secrets/actions
3. Click "New repository secret"
4. Name: CREAMAT_SSH_PASSWORD
5. Value: P@ssw0rd768
6. Click "Add secret"
